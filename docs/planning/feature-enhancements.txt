Rift Tap
    Software Improvements
        Save settings and preserve them on restart.
            default behaviour should have the simulate scans set to false

        Put overlay preview on the settings page

        Setting for battlefield cards to be displayed in landscape instead of portrait
        
        Better chip programming flows
            When card ID is displayed in search, it should show the letters in all caps so instead of ven-031 it would show VEN-031
                this is working for deck import, but not for single card search

        Add riftbound card backs (blue, white, black) for animations
		    also add toggle for showing card backs
		
    Hardware Improvements    
        Visual/audio feedback of a scan
            will need to find a small, low profile mic and led module that
        
        For pro model with a screen, add feature for touchscreen to act as a point tracker like a lifeboard
	
	
	
	